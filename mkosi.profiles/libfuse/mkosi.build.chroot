#!/bin/bash
set -e

if [[ ! -f libfuse/meson.build ]]; then
    exit 0
fi

meson setup -D prefix=/usr -D useroot=false "$BUILDDIR/libfuse" "$SRCDIR/libfuse"
meson install -C "$BUILDDIR/libfuse"

cp "$BUILDDIR/libfuse/example/passthrough_hp" $DESTDIR/usr/bin
