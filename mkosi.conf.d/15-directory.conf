# If we're creating a directory image, do not make it bootable as we build
# CONFIG_VIRTIOFS into the kernel and as such don't need to build an initrd.
[Match]
Format=directory

[Content]
Bootable=no
