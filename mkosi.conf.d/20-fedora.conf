[Match]
Distribution=fedora

[Distribution]
@Release=rawhide

[Content]
Packages=
        alsa-lib
        device-mapper
        device-mapper-multipath
        dnf
        drgn
        elfutils-libelf
        f2fs-tools
        fio-engine-libaio
        fsverity-utils
        gfs2-utils
        iputils
        kernel-core
        libasan
        libbpf
        libcap
        libcap-ng
        libguestfs-gfs2
        libmnl
        libubsan
        man-pages
        numactl-libs
        ocfs2-tools
        popt
        sqlite
        systemd-networkd
        xfsprogs-xfs_scrub
        xz

BuildPackages=
        clang-tools-extra
        e2fsprogs-devel
        gdbm-devel
        glibc-static
        kernel-headers
        libaio-devel
        libcap-static
        pkgconfig(alsa)
        pkgconfig(blkid)
        pkgconfig(cmocka)
        pkgconfig(devmapper)
        pkgconfig(fuse3)
        pkgconfig(libacl)
        pkgconfig(libattr)
        pkgconfig(libbpf-devel)
        pkgconfig(libcap-ng)
        pkgconfig(libcap)
        pkgconfig(libcap)
        pkgconfig(libelf)
        pkgconfig(libmnl)
        pkgconfig(liburing)
        pkgconfig(libzstd)
        pkgconfig(lzo2)
        pkgconfig(numa)
        pkgconfig(openssl)
        pkgconfig(popt)
        pkgconfig(systemd)
        pkgconfig(uuid)
        pkgconfig(zlib)
        python3-sphinx
        xfsprogs-devel
