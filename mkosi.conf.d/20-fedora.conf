[Match]
Distribution=fedora

[Distribution]
@Release=rawhide

[Content]
Packages=
        alsa-lib
        device-mapper
        dnf
        drgn
        elfutils-libelf
        fio-engine-libaio
        fsverity-utils
        kernel-core
        libasan
        libbpf
        libcap
        libcap-ng
        libmnl
        libubsan
        numactl-libs
        popt
        sqlite
        systemd-networkd
        xz

BuildPackages=
        clang-tools-extra
        e2fsprogs-devel
        gdbm-devel
        glibc-devel.i686
        glibc-static
        glibc-static.i686
        kernel-headers
        libaio-devel
        libbpf-devel
        libcap-static
        pkgconfig(alsa)
        pkgconfig(blkid)
        pkgconfig(cmocka)
        pkgconfig(devmapper)
        pkgconfig(fuse3)
        pkgconfig(libacl)
        pkgconfig(libattr)
        pkgconfig(libcap-ng)
        pkgconfig(libcap)
        pkgconfig(libcap)
        pkgconfig(libelf)
        pkgconfig(libmnl)
        pkgconfig(liburing)
        pkgconfig(libzstd)
        pkgconfig(lzo2)
        pkgconfig(numa)
        pkgconfig(openssl)
        pkgconfig(popt)
        pkgconfig(systemd)
        pkgconfig(uuid)
        pkgconfig(zlib)
        python3-sphinx
        xfsprogs-devel
