#!/bin/sh
set -e

useradd --root "$BUILDROOT" --create-home --user-group fsgqa
useradd --root "$BUILDROOT" --create-home --user-group fsgqa2
useradd --root "$BUILDROOT" --create-home --user-group 123456-fsgqa2

mkosi-chroot sh -c "command -v authselect >/dev/null && authselect select minimal || true"
