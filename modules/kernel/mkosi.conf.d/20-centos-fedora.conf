# SPDX-License-Identifier: LGPL-2.1-or-later

[Match]
Distribution=|centos
Distribution=|fedora

[Content]
Packages=
        dwarves
        glibc-static
        pkgconfig(alsa)
        pkgconfig(fuse)
        pkgconfig(libcap-ng)
        pkgconfig(libcap)
        pkgconfig(libelf)
        pkgconfig(libmnl)
        pkgconfig(liburing)
        pkgconfig(numa)
        pkgconfig(openssl)
        pkgconfig(popt)
        python3-docutils
