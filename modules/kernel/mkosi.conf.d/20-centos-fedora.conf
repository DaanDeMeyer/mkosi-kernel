[Match]
Distribution=|centos
Distribution=|fedora

[Content]
Packages=
        alsa-lib
        elfutils-libelf
        fuse
        libcap
        libcap-ng
        libmnl
        liburing
        numactl-libs
        openssl-libs
        popt

BuildPackages=
        glibc-static
        pkgconfig(alsa)
        pkgconfig(fuse)
        pkgconfig(libcap)
        pkgconfig(libcap-ng)
        pkgconfig(libelf)
        pkgconfig(libmnl)
        pkgconfig(liburing)
        pkgconfig(numa)
        pkgconfig(openssl)
        pkgconfig(popt)
