[Match]
Distribution=|centos
Distribution=|fedora

[Content]
Packages=
        fsverity-utils
        xfsprogs-xfs_scrub

BuildPackages=
        e2fsprogs-devel
        gdbm-devel
        kernel-headers
        libaio-devel
        pkgconfig(devmapper)
        pkgconfig(hwloc)
        pkgconfig(libacl)
        pkgconfig(libattr)
        pkgconfig(uuid)
        xfsprogs-devel
