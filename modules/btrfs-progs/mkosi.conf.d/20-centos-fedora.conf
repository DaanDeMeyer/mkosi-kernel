# SPDX-License-Identifier: LGPL-2.1-or-later

[Match]
Distribution=|centos
Distribution=|fedora

[Content]
Packages=
        e2fsprogs-devel
        libblkid
        libzstd
        lzo
        pkgconfig(blkid)
        pkgconfig(libudev)
        pkgconfig(libzstd)
        pkgconfig(lzo2)
        pkgconfig(uuid)
        pkgconfig(zlib)
        systemd-libs
        uuid
        zlib
