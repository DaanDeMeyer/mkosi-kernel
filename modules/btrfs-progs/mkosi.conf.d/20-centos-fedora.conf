[Match]
Distribution=|centos
Distribution=|fedora

[Content]
Packages=
        libblkid
        systemd-libs
        libzstd
        lzo
        uuid
        zlib

BuildPackages=
        e2fsprogs-devel
        pkgconfig(blkid)
        pkgconfig(uuid)
        pkgconfig(libzstd)
        pkgconfig(lzo2)
        pkgconfig(zlib)
        pkgconfig(libudev)
